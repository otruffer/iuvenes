<div class="list_element">
  <div class="list_element_header">
    <div class="list_element_title">
      <%= kant.title %>
    </div>
  </div>
  <div class="list_element_content">
    <%= simple_format(kant.content) %>
  </div>
  <div class="list_element_footer">
    <%= link_to 'Anzeigen', verbindung_kant_path(@verbindung, kant) %>
    <% if (current_user.root? || (current_user.admin? && current_user.verbindung = biercomment.verbindung)) then %>
        <%= link_to 'Bearbeiten', edit_verbindung_kant_url(@verbindung, kant) %>
        <%= link_to 'Löschen', verbindung_kant_path(@verbindung, kant), method: :delete, data: { confirm: 'Sind Sie sicher?' } %>
    <% end %>
  </div>
</div>